/*
    Implement a function called areThereDuplicates which accepts a variable number
    of arguments abd cgecks wherther there are any duplicates among the args 
    passed in.

    areThereDuplicates(1, 2, 3) // false
    areThereDuplicates(1, 2, 2) // true
    areThereDuplicates(a, b, c, a) // true
*/

//using two pointers to traverse args
function areThereDuplicates(...args)    {
    console.log('hi')
//     return args.sort((a, b) => a - b);
}

const run = areThereDuplicates(2, 1, 2, 4);
console.log('hi')

